export default function HomePage() {
  return (
    <main className="mx-auto max-w-4xl p-8">
      <h1 className="text-2xl font-semibold">Songwriting Dashboard</h1>
      <p className="mt-2 text-sm text-stone-600">
        Backend/domain scaffold is ready. UI implementation is intentionally deferred.
      </p>
    </main>
  );
}
